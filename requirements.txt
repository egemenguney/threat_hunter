# Threat Hunter v1.0 - Dependencies

requests>=2.20.0

# Core
python-dotenv>=1.0.0

# Async HTTP Client (fast parallel requests)
httpx>=0.25.0

# YARA signature matching
yara-python>=4.3.0

# Web Search Fallback (when GitHub API rate limited)
ddgs>=1.0.4

# YAML parsing for workflow checks / CI
PyYAML>=6.0
